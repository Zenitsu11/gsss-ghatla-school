"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginForm(){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(loading)return;setLoading(true);setError("");const form=new FormData(e.currentTarget);try{const r=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"same-origin",body:JSON.stringify({email:String(form.get("email")||"").trim(),password:String(form.get("password")||"")})});let d:any={};try{d=await r.json()}catch{}if(r.ok){window.location.href="/admin";return}setError(d.error||`Login failed (${r.status}). Please try again.`)}catch(err){console.error("Login request failed",err);setError("Login server se connect nahi ho pa raha. Thodi der baad dobara try karein.")}finally{setLoading(false)}}return <form onSubmit={submit} className="login-form"><label>Email address<input required name="email" type="email" placeholder="admin@gsssghatla.edu.in" autoComplete="username"/></label><label>Password<input required name="password" type="password" placeholder="••••••••" autoComplete="current-password"/></label>{error&&<p className="form-error">{error}</p>}<button type="submit" disabled={loading}>{loading?"Signing in…":"Sign in to dashboard"}</button></form>}
