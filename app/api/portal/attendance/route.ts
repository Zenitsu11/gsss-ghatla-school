import { NextResponse } from "next/server";
import { cookies } from "next/headers";
import { supabaseAdmin } from "@/lib/supabase";
import { readPortalSession } from "@/lib/portal-session";
export async function GET(){const s=readPortalSession((await cookies()).get("gsss_portal")?.value);if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const db=supabaseAdmin();if(!db)return NextResponse.json({error:"Supabase not configured"},{status:500});const q=s.role==="student"?db.from("attendance").select("*").eq("student_id",s.id).order("attendance_date",{ascending:false}).limit(60):db.from("attendance").select("*").order("attendance_date",{ascending:false}).limit(100);const {data,error}=await q;if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({items:data||[]});}
export async function POST(request:Request){const s=readPortalSession((await cookies()).get("gsss_portal")?.value);if(!s||s.role!=="teacher")return NextResponse.json({error:"Teacher access required"},{status:403});const db=supabaseAdmin();if(!db)return NextResponse.json({error:"Supabase not configured"},{status:500});const body=await request.json();if(!body.studentId||!body.attendanceDate||!body.status)return NextResponse.json({error:"Student, date and status are required"},{status:400});const {error}=await db.from("attendance").upsert({student_id:body.studentId,attendance_date:body.attendanceDate,status:body.status,marked_by:s.id,note:body.note||null},{onConflict:"student_id,attendance_date"});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
