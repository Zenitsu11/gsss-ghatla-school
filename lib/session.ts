import crypto from "crypto";
const secret = () => process.env.SESSION_SECRET || "change-this-secret-before-production";
export function createSession(email: string) { const signature = crypto.createHmac("sha256", secret()).update(email).digest("hex"); return Buffer.from(`${email}.${signature}`).toString("base64url"); }
export function validSession(value?: string) { if (!value) return false; try { const decoded = Buffer.from(value, "base64url").toString(); const separator = decoded.lastIndexOf("."); if (separator <= 0) return false; const email = decoded.slice(0, separator); const signature = decoded.slice(separator + 1); const expected = crypto.createHmac("sha256", secret()).update(email).digest("hex"); return signature.length === expected.length && crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected)); } catch { return false; } }
